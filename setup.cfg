[codespell]
skip = .git,./docs/build
count =
quiet-level = 3

[aliases]
test=pytest

[tool:pytest]
addopts=tests/
markers =
    regression(test_id): regression test with unique ID (RT-NNN)
    one_off(issue, test_id): one-off test tied to a specific issue (OT-NNN)
